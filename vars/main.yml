---
# vars file for devrole-ansible
opsroll:
  ansible:
    user_install_path: $HOME/.ansible
    roles_path: "{{opsroll.ansible.user_install_path}}/roles/galaxy:{{opsroll.ansible.user_install_path}}/roles/local"
    inventory_path: "{{opsroll.ansible.user_install_path}}/inventory"
    library_path: "{{opsroll.ansible.user_install_path}}/library"
    logs_path: "{{opsroll.ansible.user_install_path}}/logs"
    retry_files_save_path: "{{opsroll.ansible.user_install_path}}/retry"
    fact_caching: redis
